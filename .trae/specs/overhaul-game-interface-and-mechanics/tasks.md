# Tasks
- [x] Task 1: **服务端逻辑重构 (`src/server/game.ts`)** - 将新玩家加入房间的初始积分 (`score`) 从 1000 变更为 0。
- [x] Task 2: **玩家头像与座次卡片 UI (`src/App.tsx`)** - 引入高档次的默认头像图片（如果没现成的，通过 Base64 或外部占位图获取，比如带暗金边框的剪影），替换玩家座位上的白底/黄框纯文字块，并确保玩家积分显示 0 时不报错。
- [x] Task 3: **全局音效开关 (`src/App.tsx`)** - 在界面右上角增加一个带有音量/静音 SVG 图标的圆角按钮，点击后能够真实控制 `playSound` 方法和背景音乐是否播放。
- [x] Task 4: **游戏主桌面彻底黑化/红化 (`src/App.tsx`)** - 移除 `min-h-screen bg-slate-50` 这种廉价的白茫茫背景。引入 `qian.png` 或类似的深色高级背景，使中央“等待开局/准备开始”面板呈现高级暗黑毛玻璃态，对齐前端大门的高级拟物风格。

# Task Dependencies
- [Task 1] 是纯服务端修改，随时可做。
- [Task 2]、[Task 3] 和 [Task 4] 均是对 `App.tsx` 游戏状态 UI 的深度重构，推荐同时在同一个 Sub-Agent 中合并执行，以保证界面排版的一致性。