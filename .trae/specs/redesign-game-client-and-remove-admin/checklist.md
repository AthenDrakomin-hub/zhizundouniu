- [x] `src/App.tsx` 中不再有 `isHostMenuOpen` 状态及其弹窗（管理后台）。
- [x] `src/App.tsx` 中不再有 `RoomSettingsModal` 组件及其触发按钮（设置）。
- [x] `src/App.tsx` 中不再有 `addBot`（添加机器人）的相关按钮和事件。
- [x] “房卡状态”和“当前规则”等长条卡片已从等待界面或右下角移除，改用更为隐蔽或符合游戏的 UI。
- [x] 顶部“当前局数”、“我的积分” UI 从占面积的块状卡片简化成了轻量的游戏抬头信息。
- [x] 移除了游戏主容器和部分背景的硬编码黑色/灰色（如 `bg-[#1c1c1e]`），游戏底色受全局 `index.css` 的“发财红”控制，不再显得压抑或像后台系统。